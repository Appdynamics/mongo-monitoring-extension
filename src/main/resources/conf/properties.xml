<!--
    DB credentials are to be placed in separate <credentials> elements.
    all arguments are required.

    Example:

    <credentials>
        User should have read permissions on the given db
        <username>user</username>
        <password>guest</password>
                   OR
        <password-encrypted>encpassword</password-encrypted>
        <db>mydb</db>
    </credentials>
-->

<mongo-monitor>

</mongo-monitor>
